const chai = require('chai');
const assert = chai.assert;
const expect = chai.expect;
const should = chai.should();


describe('invoice data', () => {
    it('should make a copy of data input in text field', () => {
        assert.equal(1,2);
    })
});